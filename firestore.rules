rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Autenticados (incluindo anônimo) podem ler/escrever
    // Para uso interno - ajuste conforme necessário
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
